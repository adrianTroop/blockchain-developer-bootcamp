const { ethers } = require("hardhat");

async function main() {
    //Distribute Tokens

    //Deposit tokens to exchange

    //Make orders

    //Cancel orders

    //Fill orders
}

main()
  .then(() => process.exit(0))
  .catch((error) => {
    console.error(error);
    process.exit(1);
  });
